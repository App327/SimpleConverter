# SimpleConverter

## 📃 Краткое описание
Простой и расширяемый конвертер единиц измерения.

## 🖥️ Использование
Чтобы использовать SimpleConverter, просто откройте в браузере (или загрузите на веб-сервер и откройте через него) файл `index.html`. Этот файл и является основным файлом SimpleConverter.

## ↔️ Расширение конвертера
SimpleConverter поддерживает расширение его функциональности с помощью загрузки JSON-файла со сведениями о конвертерах. Эти JSON-файлы называются _пакетами конвертеров_. Каждый пакет конвертеров содержит информацию о каждом заданном в пакете конвертере (например, конвертер длины, массы, площади и т. д.), а внутри каждого конвертера находится список используемых конвертером единиц измерения с формулами для конвертации из одной единицы в другую.

По умолчанию в SimpleConverter задано 3 конвертера:

* **Длина**
    * Сантиметры (см)
    * Метры (м)
    * Километры (км)
* **Площадь**
    * Квадратные сантиметры (см²)
    * Квадратные метры (м²)
    * Квадратные километры (км²)
* **Масса**
    * Граммы (г)
    * Килограммы (кг)

Эти конвертеры из базовой поставки предназначены для проверки, тестирования и отладки функциональности SimpleConverter. Чтобы конвертировать единицы измерения для повседевного использования, нужно загрузить в SimpleConverter пакет конвертеров. Такой пакет можно легко создать самому или скачать из Интернета. В будущем в базовую поставку SimpleConverter будет входить большинство используемых на данный момент единиц измерения.

>[!WARNING]
>Перед тем, как использовать скачанный из Интернета пакет конвертеров, **убедитесь в его безопасности**!

## 📥 Загрузка пакетов конвертеров
На данный момент, в SimpleConverter надо загружать JSON-файл с пакетом конвертеров при каждом открытии SimpleConverter.

Чтобы загрузить пакет конвертеров в SimpleConverter, выполните следующие действия:

1. Нажмите на значок шестерёнки в правом верхнем углу. Откроется окно «Настройки».
2. В «Настройках» найдите раздел _Загрузка пакета данных_.
3. В разделе _Загрузка пакета данных_ нажмите кнопку «Выберите файл», а затем выберите JSON-файл с пакетом конвертеров.
4. Как только вы выберете файл, справа от поля выбора файла включится кнопка «Применить». Нажмите на эту кнопку.
5. **Готово!** Теперь можно закрыть окно настроек (для этого нажмите на крестик в правом верхнем углу) и пользоваться конвертерами пакета только загруженного пакета конвертеров.

>[!TIP]
>Чтобы откатить изменения, сделанные пакетом конвертеров и вернуть содержимое из базовой поставки SimpleConverter, нажмите кнопку «Вернуть стандартные данные» в настройках в разделе _Загрузка пакета данных_.

## 🆕 Создание своего JSON-пакета конвертеров
Чтобы загруженногособственный пакет конвертеров для SimpleConverter, создайте файл, расширением имени которого является `.json`. В этот файл вставьте вот этот образец содержимого JSON-пакета конвертеров:

```json
{
  "format": 1,
  "data": {
    "1": {
      "name": "объём",
      "units": ["см³", "м³"],
      "units_full": ["кубические сантиметры", "кубические метры"],
      "formulas": {
        "см³-м³": "$ / 1000000",
        "м³-см³": "$ * 1000000"
      }
    }
  }
}
```

Поле `format` в этом JSON обозначает версию формата данных в виде числа. Благодаря этому полю, SimpleConverter сможет обработать JSON-файл в соответствии с заданным форматом.

В поле `data` содержится само содержимое пакета конвертеров. Значением этого поля является объект, в котором перечислены все конвертеры, которые нужно загрузить в SimpleConverter. Объект `data` содержит другие объекты, описывающие конкретные конвертеры. В качестве названий у полей с объектами используются инкрементальные натуральные числа. В каждом объекте с конвертером указываются следующие поля:

* `name` — отображаемое название конвертера (с маленькой буквы)
* `units` — массив с единицами измерения, которые используются в конвертере (в массиве перечисляются сокращения названий единиц)
* `units_full` — полные названия единиц измерения, перечисленные в том же порядке, что и в `units`
* `formulas` — объект с формулами для конвертации. В качестве названий полей объекта используются строки в формате `X-Y`, где **X** и **Y** — единицы измерения из массива `units`; **X** — единица, из которой выполняется конвертация, а **Y** — единица, в которую выполняется конвертация. В качестве значений полей объекта `formulas` указывается строка, содержащая пример для вычисления результата; для подстановки в пример введённого пользователем числа используется символ доллара (`$`).

Пример выше устанавливает в SimpleConverter 1 конвертер под названием «Объём», в котором используются единицы измерения _кубические сантиметры (см³)_ и _кубические метры (м³)_, для конвертации из см³ в м³ введённое пользователем число делится на 1 000 000, а для конвертации из м³ в см³ введённое число умножается на 1 000 000.

## 📝 Важные примечания
* Загрузка пакета конвертеров полностью перезаписывает конвертеры из базовой поставки SimpleConverter. В будущих версиях SimpleConverter будет добавлена возможность сохранять базовые конвертеры.
* В формулах технически можно использовать любой JavaScript-код, однако это крайне не рекомендуется использовать. Такое может быть полезно для сложных вычислений, для которых не получится использовать простой пример. Настоятельно рекомендуется проверять содержимое кода в формулах, особенно если пакет конвертеров скачан из Интернета. В будущем прямое использование кода заменится на безопасные методы, позволяющие хорошо и безопасно вычислять результаты конвертации.
* В будущих версиях SimpleConverter будет добавлено больше вариантов указания формул. Сейчас нужно указывать формулы для каждого случая конвертации, что может быть непрактичным для большого количества единиц измерения.

-----

_Приятного пользования SimpleConverter! 😊_
