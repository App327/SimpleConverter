# SimpleConverter

## 📃 Краткое описание
Простой и расширяемый конвертер единиц измерения.

## 🖥️ Использование
Чтобы использовать SimpleConverter, просто откройте в браузере (или загрузите на веб-сервер и откройте через него) файл `index.html`. Этот файл и является основным файлом SimpleConverter.

## ↔️ Расширение конвертера
SimpleConverter поддерживает расширение его функциональности с помощью загрузки JSON-файла со сведениями о конвертерах. Эти JSON-файлы называются _пакетами конвертеров_. Каждый пакет конвертеров содержит информацию о каждом заданном в пакете конвертере (например, конвертер длины, массы, площади и т. д.), а внутри каждого конвертера находится список используемых конвертером единиц измерения с формулами для конвертации из одной единицы в другую.

По умолчанию в SimpleConverter задано 2 конвертера:

* **Длина**
    * Сантиметры (см)
    * Метры (м)
    * Километры (км)
* **Площадь**
    * Квадратные сантиметры (см²)
    * Квадратные метры (м²)

Эти конвертеры из базовой поставки предназначены для проверки, тестирования и отладки функциональности SimpleConverter. Чтобы конвертировать единицы измерения для повседевного использования, нужно загрузить в SimpleConverter пакет конвертеров. Такой пакет можно легко создать самому или скачать из Интернета. В будущем в базовую поставку SimpleConverter будет входить большинство используемых на данный момент единиц измерения.

>[!WARNING]
>Перед тем, как использовать скачанный из Интернета пакет конвертеров, **убедитесь в его безопасности**!

## 📥 Загрузка пакетов конвертеров
На данный момент, в SimpleConverter надо загружать JSON-файл с пакетом конвертеров при каждом открытии SimpleConverter.

Чтобы загрузить пакет конвертеров в SimpleConverter, выполните следующие действия:

1. Нажмите на значок шестерёнки в правом верхнем углу. Откроется окно «Настройки».
2. В «Настройках» найдите раздел _Загрузка параметров конвертера_.
3. В разделе _Загрузка параметров конвертера_ нажмите кнопку «Выберите файл», а затем выберите JSON-файл с пакетом конвертеров.
4. Как только вы выберете файл, справа от поля выбора файла включится кнопка «Применить». Нажмите на эту кнопку.
5. **Готово!** Теперь можно закрыть окно настроек (для этого нажмите на крестик в правом верхнем углу) и пользоваться конвертерами из загруженного только что пакета конвертеров.

## 🆕 Создание своего JSON-пакета конвертеров
Чтобы создать свой собственный пакет конвертеров для SimpleConverter, создайте файл, расширением имени которого является `.json`. В этот файл вставьте вот этот образец содержимого JSON-пакета конвертеров:

```json
{
  "format": 1,
  "data": {
    "1": {
      "name": "объём",
      "units": ["см³", "м³"],
      "units_full": ["кубические сантиметры", "кубические метры"],
      "formulas": {
        "см³-м³": "$ / 1000000",
        "м³-см³": "$ * 1000000"
      }
    }
  }
}
```

Поле `format` в этом JSON обозначает версию формата данных в виде числа. Благодаря этому полю, SimpleConverter сможет обработать JSON-файл в соответствии с заданным форматом.

В поле `data` содержится само содержимое пакета конвертеров. Значением этого поля является объект, в котором перечислены все конвертеры, которые нужно загрузить в SimpleConverter. Объект `data` содержит другие объекты, описывающие конкретные конвертеры. В качестве названий у полей с объектами используются инкрементальные натуральные числа. В каждом объекте с конвертером указываются следующие поля:

